# =============================================================================
# Spell Fulfillment - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values
# For production, ensure all required values are set securely

# =============================================================================
# Application Settings
# =============================================================================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-random-secret-key

# Environment: development, production, testing
ENVIRONMENT=development

# Debug mode (set to false in production)
DEBUG=true

# =============================================================================
# Database Configuration
# =============================================================================
# For local development:
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/spells

# For Docker Compose (uses service name 'db'):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/spells

# Database pool settings (optional)
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# =============================================================================
# Docker/Production Database Settings
# =============================================================================
# Used by docker-compose.prod.yml
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-secure-password
POSTGRES_DB=spells

# =============================================================================
# Etsy API Configuration
# =============================================================================
# Get from https://www.etsy.com/developers/your-apps
ETSY_API_KEY=your-etsy-api-key
ETSY_API_SECRET=your-etsy-api-secret

# Callback URL (update for production domain)
ETSY_REDIRECT_URI=http://localhost:8000/api/v1/etsy/auth/callback

# How often to poll for new orders (minutes)
# ETSY_POLL_INTERVAL_MINUTES=5

# =============================================================================
# Anthropic Claude API Configuration
# =============================================================================
# Get from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# Model settings (optional)
# CLAUDE_MODEL=claude-sonnet-4-20250514
# CLAUDE_MAX_TOKENS=1024

# =============================================================================
# SendGrid Email Configuration
# =============================================================================
# Get from https://sendgrid.com/
SENDGRID_API_KEY=your-sendgrid-api-key

# Sender information
FROM_EMAIL=spells@yourdomain.com
FROM_NAME=Mystic Spells

# =============================================================================
# Authentication Settings (optional)
# =============================================================================
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# =============================================================================
# Docker Production Settings
# =============================================================================
# Port to expose the app on
APP_PORT=8000

# Auto-run migrations on startup (default: true)
# RUN_MIGRATIONS=true

# Auto-create admin user on startup (optional)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=secure-admin-password
